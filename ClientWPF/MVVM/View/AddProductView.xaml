<UserControl x:Class="ClientWPF.MVVM.View.AddProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientWPF.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="705" d:DesignWidth="955"
             Loaded="UserControl_Loaded">
    <DockPanel>

        <WrapPanel DockPanel.Dock="Top">
            <TextBlock Text="Products"
                       Foreground="White"
                       FontSize="30"
                       HorizontalAlignment="Left"
                       Margin="5 10"/>
            <ComboBox x:Name="categoriesList" DockPanel.Dock="Left" Margin="30 0" Width="200" ItemsSource="{Binding Producers}" DisplayMemberPath="Name" VerticalAlignment="Center" FontSize="20" SelectedItem="{Binding SelectedProducer}"/>
            <ComboBox x:Name="producersList" DockPanel.Dock="Left" Margin="5 0" Width="400" ItemsSource="{Binding Categories}" DisplayMemberPath="Name" VerticalAlignment="Center" FontSize="20" SelectedItem="{Binding SelectedCategory}"/>
        </WrapPanel>
        <StackPanel Orientation="Vertical">
            <Border HorizontalAlignment="Center"
                    Margin="0, 40"
                    Width="250" Height="250"
                    CornerRadius="35"
                    BorderThickness="1.6" BorderBrush="Gray"
                    Grid.Column="1">
                <Border.Background>
                    <ImageBrush ImageSource="/Images/user-profile.jpg"/>
                </Border.Background>
            </Border>
            <TextBlock Foreground="White" Text="{Binding Counter, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Foreground="Black" Text="{Binding Counter, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding Description}" Foreground="Aqua"/>
            <Button Command="{Binding OpenFileDialog}">Додати</Button>
            <TextBlock Text="{Binding Description}" Foreground="Aqua"/>

        </StackPanel>
    </DockPanel>
</UserControl>
